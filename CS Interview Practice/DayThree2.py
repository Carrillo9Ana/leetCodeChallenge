class Solution:
    def decodeString(self, s: str) -> str:
        # the letter inside the brackets is being multiplied K number of times
        # k > 0
        # probably splice the string into idividual strings
        # return the string decoded
        
        c = list()
        for x in s:
            c.append(x)
            if i in x == '['
            
            
        print(c)